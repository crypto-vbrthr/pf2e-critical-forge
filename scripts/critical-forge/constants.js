export const CARD_CATEGORIES = Object.freeze([
  "criticalHit",
  "criticalFumble",
  "spellCriticalHit",
  "spellCriticalFumble",
  "savingThrowCriticalSuccess",
  "savingThrowCriticalFailure"
]);

export const EFFECT_TARGETS = Object.freeze([
  "target",
  "source"
]);

export const CARD_ID_PATTERN = /^[a-z0-9][a-z0-9._-]*$/;
